import {groq} from 'next-sanity'

export const resumeQuery = groq`
*[_type == "resume"][0]{
	_id,
	fullName,
	headline,
	location,
	summary,
	experiences[]->{
		_id,
		role,
		organization,
		location,
		employmentType,
		industryFocus,
		summary,
		period,
		engagementStatus,
		highlights,
		tooling
	},
	projects[]->{
		_id,
		title,
		role,
		objective,
		summary,
		period,
		status,
		highlights,
		technologies,
		links
	},
	education[]->{
		_id,
		degree,
		institution,
		location,
		studyType,
		period,
		classStanding,
		modules,
		summary
	},
	publications[]->{
		_id,
		title,
		publisher,
		location,
		category,
		publishedAt,
		summary,
		citation,
		links
	},
	certifications[]->{
		_id,
		name,
		issuer,
		location,
		issuedAt,
		credentialId,
		verificationUrl
	},
	skillCategories[]->{
		_id,
		label,
		skills
	},
	languages[]->{
		_id,
		name,
		proficiency
	}
}`
